#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    auto b = true;       // a bool
    auto ch = 'x';       // a char
    auto i = 123;        // an int
    auto d = 1.2;        // a double
    auto z = sqrt(d);    // z has the type of whatever sqrt(y) returns
    auto bb{true};      // bb is a bool    
}
